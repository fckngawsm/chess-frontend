import { Chessboard } from "react-chessboard";
import { StyledWrapperBoard } from "./styled";

export const ChessBoard = () => {
  return (
    <StyledWrapperBoard>
      <Chessboard />
    </StyledWrapperBoard>
  );
};
